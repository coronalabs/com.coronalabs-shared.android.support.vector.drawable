Sources for the plugin `shared.android.support.vector.drawable`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.vector.drawable" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
